---
---

Add a regression test for issue #53 (beforeOpen ran twice on link navigation). The fix itself shipped with the navigation-lifecycle rework (#82); this only locks the behavior in with a test, so no release is needed.
