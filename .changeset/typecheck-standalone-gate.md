---
---

<!-- CI and repository configuration only: make `pnpm typecheck` resolve
workspace packages from source so it passes without a prior build, and guard it
with `verify:typecheck-standalone`. -->
