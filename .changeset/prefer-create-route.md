---
"@effector/router": patch
---

Use the shared `createRoute()` pathless lifecycle for groups and chained routes,
and migrate examples, fixtures, and binding tests away from the deprecated
`createVirtualRoute` factory.
